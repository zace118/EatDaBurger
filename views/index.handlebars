{{!-- Setup this file to have the template that Handlebars can render onto --}}
<img src="./../public/assets/img/krabby-patty.png">


<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{burger_name}} <button id="devoured">Devoured</button> <br>
    {{/unless}}
    {{/each}}
</ul>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    devoured {{burger_name}} <br>
    {{/if}}
    {{/each}}
</ul>

<h2>Eat Da Burger!!!</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger:</label>
        <input type="text" id="ca" name="name">
    </div>

    {{!-- Create a button that will submit the user input into the database --}}
    <button type="submit">Burger-fy me!</button>
</form>